// Currently empty, but can be used for utilities like currency formatting,
// product rendering helpers, etc.
// Example: format currency
window.formatCurrency = function(amount) {
    return '$' + amount.toFixed(2);
};
